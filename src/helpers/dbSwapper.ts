// import { DATA_SOURCE } from "../../db/multiClient";

// export const convertToType = (id: string) => {
//     if (DATA_SOURCE === "postgres") {
//         return Number(id)
//     } else {
//         return id
//     }
// }